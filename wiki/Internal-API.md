# Internal API

Internal contract helpers normalize result/fact representation, validate property combinations, constrain optional operations structurally, validate provider metadata and implement common enumeration/result vocabulary without introducing a runtime storage registry.
